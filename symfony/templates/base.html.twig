<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}

            {% for city in cities %}
                <p>
                Processed city {{ city['cityName'] }} | {{ city['todayWeather'] }} - {{ city['tomorrowWeather'] }}
                </p>
            {% endfor %}

        {% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
